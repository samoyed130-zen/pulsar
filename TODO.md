# PULSAR — 企画・TODO

**ZEN Study プログラミングコンテスト 2026 夏 / Web ページ部門**

作成: 2026-09-13 21:10 / 締切: 本日 23:59

---

## 0. 現在地（このファイルは作業の共有状態。着手・完了のたびに更新すること）

| 項目 | 内容 |
| --- | --- |
| 最終更新 | 2026-09-13 21:40 |
| 進行中 | C. シーンの演出詰め（見た目の作り込み） |
| 完了 | 企画確定 / 環境構築 / git + GitHub push / T. テスト基盤（**92件すべて成功**） / A. 骨組み / B. 音 / C. シーン5種の初版 / D. 触れる区間の初版 |
| 次 | 実機での見た目確認 → 演出の詰め → スマホ確認 → Pages 公開確認 |
| ブロッカー | **GitHub Pages の有効化は本人操作が必要**（Settings → Pages → main / root） |

**引き継ぎメモ**

- 環境: Windows / PowerShell。git 2.55 / gh 2.100 / Node 24.19 を導入済み。
- リモート: `git@github.com:samoyed130-zen/pulsar.git`（SSH 認証確認済み・push 済み）。
  鍵は `C:\Users\norm8\.ssh\id_ed25519_samoyed130_zen`（**パスフレーズなし**。
  旧鍵はパスフレーズ失念のため `.bak` へ退避、GitHub 側も新しい鍵に登録済み）。
  リポジトリ単位で `core.sshCommand` を設定してあるので、追加の設定なしに push できる。
- 公開 URL（Pages 有効化後）: https://samoyed130-zen.github.io/pulsar/
- **PowerShell から git/node/gh を呼ぶ前に PATH を更新すること**（導入直後のセッションでは通らない）:
  ```powershell
  $env:Path = [System.Environment]::GetEnvironmentVariable("Path","Machine") + ";" + [System.Environment]::GetEnvironmentVariable("Path","User")
  ```
- テストの実行方法は2通り。どちらも同じテストを走らせる:
  - コマンドライン（開発中はこちら）: `node test/run-node.js`
  - ブラウザ（審査員向け・本番）: `test.html` を開く
- ローカル確認用サーバー: `python -m http.server 8123` を起動済み → http://127.0.0.1:8123/
- git の `user.name` を暫定で `norm8` に設定。**本名 or ハンドル名を確認して差し替える**。
- `LICENSE` の著作権者が `NAME_HERE` のまま。提出前に置換が必要。
- リモートリポジトリは未作成（`gh auth login` も未実施）。デプロイ時に行う。

### このファイルの更新ルール（複数エージェントで作業する場合を含む）

1. 作業に着手したら §0 の「進行中」を書き換える
2. 項目が終わったら該当チェックボックスを `[x]` にする
3. 終わったら §10 進捗ログに 1 行追記する（時刻・何を・どのファイル）
4. 判断を変えたときは §9 判断メモに理由を残す。**決定の理由を口頭で終わらせない**
5. 未完のまま中断するときは §0 のブロッカーに「何が途中か」を具体的に書く

---

## 1. コンセプト

**触れる メガデモ。**

自動再生されるデモ（複数のビジュアルエフェクトが音楽に合わせて切り替わる）が流れ、
どこでも画面に触れた瞬間、そのエフェクトがそのままミニゲームの舞台になる。
遊び終えると、何事もなかったようにデモの続きに戻る。

- **見るだけで成立する** — 開いて3秒で絵が動いている。説明を読まなくてよい
- **触ると遊べる** — 操作は「指1本で左右」だけ。説明を読まなくてよい
- **音も自作** — Web Audio で合成するので音源ファイルが不要（著作権クリア）

一行で言うと「**デモに触ると、ゲームになる**」。

### なぜこれなのか

- 90年代デモシーンの表現を、ライブラリ禁止という条件の下で素の JS で書く
  → 禁止ルールが不利ではなく見せ場になる
- 音楽まで合成なので、素材のライセンス確認項目が全部ゼロになる
- 審査員が触らなくても、触っても、どちらでも作品が伝わる

---

## 2. 構成

**優先順位は「見た目 ＞ ゲーム性」。** ゲームはデモを中断させる別画面ではなく、
デモの流れの中の一区間として扱う。

```
[起動] → タイトル（ロゴ + 星の加速）
   ↓ 自動
[S1] スターフィールド          … 音の立ち上がり、ビートで加速
   ↓ ホワイトフラッシュ
[S2] プラズマ                  … 画面全体が色で脈打つ
   ↓ ワイプ
[S3] トンネル                  ← ★触れている間だけ操作できる区間
   ↓
[S4] メタボール / ロトズーム    … 濃い見せ場
   ↓ 縦ブラインド
[S5] コッパーバー + 横スクロール文字  … 作者名・グリーティング
   ↓ ループして S1 へ
```

**触れる区間（TUNNEL RUN）**
S3 の間だけ、指を左右に動かすと自機が動き、リングの切れ目を通り抜けられる。
触らなければ自動操縦でデモとして流れ続ける。ぶつかっても終わらせず、
画面を赤く弾けさせて即座に走行を再開する（**止めない・説明しない・デモを壊さない**）。
到達距離をスコアとして隅に小さく出す。

---

## 3. タイムライン（切り捨てライン付き）

| 時刻 | やること | 遅れたら |
| --- | --- | --- |
| 21:05–21:20 | **A. 骨組み** キャンバス・ループ・シーン管理・入力 | 落とせない |
| 21:20–21:35 | **T. テスト基盤** `test/runner.js` + `mathx.js` とそのテスト | 落とせない（後から作れない） |
| 21:35–21:55 | **B. 音** 内蔵シンセ + BPM + ビート通知 | → 音なしで進む（§7） |
| 21:55–22:50 | **C. シーン5種 ＋ 遷移演出** | → 3種でループ（§7） |
| 22:50–23:10 | **D. 触れる区間** ＋ 判定のテスト | → 自動操縦のまま公開（§7） |
| 23:10–23:25 | **E. つなぎ・スマホ確認・テスト全緑** | 落とせない |
| 23:25–23:45 | **F. GitHub Pages デプロイ** | 落とせない |
| 23:45–23:59 | **G. 予備・最終確認** | — |

> テスト基盤を先頭近くに置くのは、純粋関数を**切り出してから**実装するため。
> 順序を逆にすると Canvas 描画と判定が絡んでテスト不能になる。

> **23:20 の判断点**: この時刻にゲームが動いていなければ、シーンを増やすのを止めて
> 残り全部をゲームとデプロイに回す。

---

## 4. TODO

### A. 骨組み（21:10–21:30）

- [ ] 既存の「声で、崩す。」の3ファイルを `archive/` へ退避（作業を混ぜない）
- [ ] `index.html` / `style.css` / `main.js` を新規作成
- [ ] フルスクリーン Canvas + DPR 対応 + リサイズ
- [ ] `requestAnimationFrame` ループ、経過時間 `t` と デルタ `dt` を配る
- [ ] シーン管理: `{ name, duration, draw(t, ctx, beat) }` の配列を順に再生
- [ ] シーン切り替え時のクロスフェード（黒フェード or ホワイトフラッシュ）
- [ ] Pointer Events で「押した位置」「ドラッグ中の x」を常時取得
- [ ] 画面のどこかを押したら `startGame()` が呼ばれる導線（中身は後で）

### B. 音（21:30–21:55）

- [ ] `AudioContext` を最初のタップで起動（自動再生ポリシー対策・必須）
- [ ] ベース音: `OscillatorNode`（saw）+ `BiquadFilterNode`（lowpass）
- [ ] リード音: square、シンプルな4〜8音のループ
- [ ] ドラム: ノイズバッファ + 短いエンベロープでハイハット、sine のピッチ下降でキック
- [ ] BPM（126程度）で拍を刻み、各シーンへ `beat` / `bar` を通知
- [ ] ミュートボタン（audio 起動前は「♪ 音を出す」表示）
- [ ] 音量は控えめに（初見で驚かせない）

### C. シーン（21:55–22:50）★ここが作品の主役

- [ ] **S1 スターフィールド** — 3D の点を透視投影。ビートで加速、奥ほど暗く細く
- [ ] **S2 プラズマ** — sin の重ね合わせ。`ImageData` を直接書き換え、低解像度で描いて拡大
- [ ] **S3 トンネル** — 奥行き方向に並べたリング。ねじれと呼吸する半径
- [ ] **S4 メタボール** — 距離場のしきい値。低解像度 + 拡大でにじませる
- [ ] **S5 コッパーバー + スクローラー** — 横帯の走査 + 正弦波で波打つ横スクロール文字

演出（**ここを削ると一気に安っぽくなる。シーンを1つ削ってでも残す**）

- [ ] 共通パレットから色を引き、時間で色相を回す（全シーンが一本の作品に見える）
- [ ] ビートに合わせた白フラッシュ・画面の微小なズーム（キックで1%だけ拡大）
- [ ] 残像（毎フレーム半透明の黒を重ねる）と加算合成による発光
- [ ] 走査線 + 周辺減光を CSS のオーバーレイで全体にかける（CRT 風、実装が安い割に効く）
- [ ] シーン遷移を3種類用意して使い分ける（白フラッシュ / ワイプ / 縦ブラインド）
- [ ] 各シーンの尺は 8〜12秒。長いと飽きる

### D. 触れる区間 TUNNEL RUN（22:50–23:10）

見た目優先のため、**ゲームとして作り込まない**。デモを止めないことを最優先にする。

- [ ] 自機（トンネル断面上の角度で位置を持つ）を S3 に重ねる
- [ ] 触れていない間は自動操縦で切れ目を通る（放置でもデモとして成立する）
- [ ] ドラッグ / マウス移動 / 左右キー で自機を動かす
- [ ] 切れ目のあるリングを一定間隔で生成、手前へ流す
- [ ] 当たり判定（自機の角度が切れ目の角度範囲に入っているか）→ `mathx` でテスト
- [ ] 衝突しても止めない。赤いフラッシュ + 画面の揺れを出して走行継続
- [ ] 到達距離を隅に小さく表示（UI を大きくしない。絵を邪魔させない）
- [ ] 「指を左右に」を S3 開始の2秒だけ薄く出す

### E. つなぎ・確認（23:10–23:25）

- [ ] デモ ⇄ ゲームの遷移が壊れないか往復して確認
- [ ] スマートフォン表示（縦・横）でレイアウトが崩れないか
- [ ] タッチで確実に操作できるか（`touch-action: none` 必須）
- [ ] Console エラーがゼロか
- [ ] 低スペックでもカクつかないよう、粒子数・解像度を画面サイズで調整
- [ ] 30秒放置しても落ちない・メモリが増え続けないか

### F. デプロイ（23:25–23:45）

- [ ] GitHub でリポジトリ作成（**Public**）
- [ ] Web 画面からファイルをアップロード（Git 未インストールのため）
- [ ] Settings → Pages → Branch: `main` / root → Save
- [ ] 数分待って公開 URL を開き、**スマホの Chrome でも**動作確認
- [ ] 外部ファイルの 404 がないか（パスは全て相対で書く）
- [ ] README に公開 URL を記載

### G. 提出（23:45–23:59）

- [ ] 応募フォームに GitHub リポジトリ URL
- [ ] 応募フォームに 公開 Web ページ URL
- [ ] 提出コードと公開ページの内容が一致していることを確認
- [ ] §8 のチェックリストを通す

---

## 5. 技術方針（コンテスト規定）

**使用可**: HTML / CSS / JavaScript（Vanilla）, Canvas 2D, Web Audio API, Pointer Events, localStorage

**使用しない**: React / Vue / Three.js / GSAP / anime.js / PixiJS / jQuery / その他外部 JS ライブラリ
Bootstrap・Tailwind は利用可だが、この作品では不要なので使わない。

**素材**: 画像・音源・フォントファイルを一切使わない。図形と合成音のみ。
→ ライセンス確認項目が構造的にゼロになる。

**秘密情報**: API キー・パスワード・個人情報を含めない。通信を一切行わない。

---

## 6. ファイル構成（予定）

```
index.html
style.css
src/
  main.js       ループ・シーン管理・入力
  scenes.js     各エフェクト
  game.js       TUNNEL RUN
  sound.js      内蔵シンセ
  mathx.js      純粋関数（角度・補間・当たり判定・スコア）
test.html       単体テストを走らせるページ
test/
  runner.js     自作テストランナー（依存ゼロ）
  mathx.test.js
  game.test.js
  scenes.test.js
Doxyfile        ドキュメント生成設定
README.md
LICENSE
archive/        旧作「声で、崩す。」
```

> 1ファイルに詰め込まない。審査でソースを読まれる前提で分ける。

---

## 6.5. コード品質の仕組み

審査対象にコード品質が含まれるため、**書き方を規約として先に決めてから実装する**。
後から整形し直す時間はない。

### 6.5.1 コメント規約（Doxygen / JSDoc 形式）

Doxygen が JavaScript を解釈できる `/** ... */` 形式で統一する。

- 各ファイルの先頭に `@file` と `@brief`
- 関数・クラスには `@brief` / `@param` / `@returns`、必要に応じて `@throws`
- 単位や範囲が自明でない引数には必ず書く（例: 角度は**ラジアン**、`level` は **0..1**）
- 「何をしているか」ではなく「**なぜそうしたか**」を書く。コードを読めば分かることは書かない
- 内部だけで使うものには `@private`

```js
/**
 * @file game.js
 * @brief トンネルを潜り抜けるミニゲームの状態と更新処理。
 */

/**
 * @brief 自機がリングの切れ目を通過できるか判定する。
 * @param {number} shipAngle  自機の角度 [rad]（0..2π に正規化済みであること）
 * @param {number} gapCenter  切れ目の中心角 [rad]
 * @param {number} gapWidth   切れ目の開き角 [rad]
 * @returns {boolean} 通過できるなら true
 */
function canPass(shipAngle, gapCenter, gapWidth) { ... }
```

- [ ] 規約をこの節のとおり全ファイルで守る
- [ ] `Doxyfile` を置く（`OPTIMIZE_OUTPUT_JAVA = YES` / `EXTENSION_MAPPING = js=Java` / `INPUT = src`）
      ※ Doxygen 未インストールでも、設定を同梱しておけば意図が伝わる。生成は任意

### 6.5.2 単体テストの仕組み

Node.js が未インストールのため、**ブラウザで走る依存ゼロのテストランナー**を自作する。
`test.html` を開くと結果が一覧表示される。GitHub Pages 上にも置くので、
審査員が URL を開くだけでテストの通過を確認できる。

- [x] `test/runner.js` に `describe` / `it` / `expect` 相当を最小限で実装
      （`toBe` / `toBeCloseTo` / `toEqual` / `toBeTrue` / `toBeFalse` / `toThrow`）
- [x] 失敗時は赤字で「期待値・実際の値・テスト名」を表示
- [x] 合計 / 成功 / 失敗の件数を上部に表示、1件でも落ちたら赤いバナー
- [x] `test.html` から各 `*.test.js` を読み込むだけで登録される形にする
- [x] `test/run-node.js` を追加（同じテストをコマンドラインからも実行できるようにした）

**テスト対象は純粋関数に限る**（Canvas・音・DOM はテストしない。時間内に見合わない）。
そのために、判定や計算を `mathx.js` とゲームの更新関数へ**先に切り出してから**実装する。

- [x] `clamp(v, lo, hi)` — 範囲外・境界値
- [x] `lerp(a, b, t)` — t=0, 1, 中間
- [x] `approach(cur, target, rate, dt)` — fps 非依存の追従
- [x] `wrapAngle(a)` — 負の角度・2π超え・ちょうど 0 と 2π
- [x] `angleDist(a, b)` — 境界（0 と 2π 付近）をまたぐ場合が本命
- [x] `canPass()` — 切れ目の中心 / 端 / わずかに外 / 境界またぎ
- [x] `scoreFromDistance()` — 単調非減少であること
- [x] `pickScene(timeline, t)` — 尺の合計を超えたらループ先頭に戻ること
- [x] `beatAt(bpm, t)` / `beatPhase(bpm, t)` — 拍番号と拍内位相
- [x] `edgeFade()` / `easeInOut()` / `hsl()`

> **テストが実際にバグを1件見つけた。** `canPass` の境界判定が浮動小数の誤差で裏返っていた
> （`1.3 - 1.0 = 0.30000000000000004`）。許容誤差 `ANGLE_EPSILON` を導入して修正済み。

> テストは実装の**直後**に書く。まとめて最後に書くと必ず時間切れになる。

### 6.5.3 その他の品質項目

- [ ] 全ファイル `'use strict';`
- [ ] グローバルを汚さない（各ファイルを IIFE で包み、`window.PULSAR` 名前空間にだけ公開）
- [ ] マジックナンバーを関数内に直書きしない。ファイル冒頭の設定オブジェクトに集約
- [ ] 命名を統一（時刻 `t` は秒、フレーム差分 `dt` は秒、角度は `rad` 接尾辞）
- [ ] 1関数は画面に収まる長さ。超えたら分ける
- [ ] Console にエラー・警告を残さない

---

## 7. 削る順番（時間が足りなくなったら上から捨てる）

1. 自機の操作（自動操縦のまま公開する。デモとしては成立する）
2. S4 メタボール
3. 音（無音でも成立するよう、エフェクトは時間で動かし、ビートは内部タイマーで代用できる設計にしておく）
4. S5 コッパーバー + スクローラー
5. 遷移演出を1種類（白フラッシュ）だけにする

**絶対に削らないもの**: スターフィールド / プラズマ / トンネル / 共通パレットと発光・残像 /
CRT オーバーレイ / スマホ動作 / デプロイ / テスト基盤と `mathx` のテスト

> 見た目優先の方針により、**操作より絵を残す**。ゲーム要素が消えても作品は成立するが、
> 絵が地味だと作品ごと成立しない。

> テストは「削る」のではなく**対象を狭める**。ランナーと当たり判定のテストだけは必ず残す。
> 0件のテストページを置く方が、無い場合より印象が悪い。

---

## 8. 提出前チェックリスト

- [ ] 十分なオリジナリティがある（チュートリアルの写しではない）
- [ ] 禁止された外部ライブラリを使っていない
- [ ] 素材の著作権・ライセンスに問題がない（＝素材を使っていない）
- [ ] 個人情報・APIキー・パスワードを含まない
- [ ] 動きのある Web ページになっている
- [ ] GitHub リポジトリが Public
- [ ] ホスティング済み、提出コードと内容が一致
- [ ] PC 版 Chrome で確認
- [ ] スマートフォン版 Chrome で確認
- [ ] Console エラーなし
- [ ] 審査期間中、公開状態を維持できる

---

## 9. 判断メモ

- **なぜ「声」案を捨てたか** — マイク許可という障壁が体験の最初に来るため、
  審査員が触らずに離脱する危険が高い。直感性で劣ると判断した。
- **なぜデモにゲームを付けるか** — 見るだけの作品は印象に残りにくく、
  ゲームだけだと技術的な見せ場が伝わりにくい。両方を1つの舞台で繋ぐのが本作の主張。
- **音を自作する理由** — 表現のためだけでなく、ライセンス項目を潰すため。
- **見た目をゲーム性より優先する理由** — 審査は短時間で多数の作品を見る。
  操作を覚えないと良さが分からない作品は不利。絵は開いた瞬間に伝わる。
- **テストをブラウザで走らせる理由** — Node が未インストールで、今から入れる時間がない。
  結果的に、審査員が URL を開くだけでテスト通過を確認できる利点が生まれた。

---

## 10. 進捗ログ

| 時刻 | 内容 | 対象 |
| --- | --- | --- |
| 20:50 | 別案「声で、崩す。」を実装（後に破棄） | — |
| 21:05 | 企画を PULSAR（メガデモ + 触れる区間）へ変更 | `TODO.md` |
| 21:07 | コード品質の仕組みを追加（Doxygen 形式 / 自作テストランナー） | `TODO.md` |
| 21:08 | 見た目優先へ方針変更。ゲームを「触れる区間」に格下げ | `TODO.md` |
| 21:11 | 旧作のファイルを削除 | `index.html` `style.css` `script.js` `README.md` |
| 21:12 | §0 現在地・§10 進捗ログを新設（引き継ぎ用） | `TODO.md` |
| 21:20 | git 2.55 / gh 2.100 / Node 24.19 を winget で導入 | 環境 |
| 21:22 | git リポジトリ初期化・初回コミット `d09362b` | `.gitignore` |
| 21:30 | 純粋関数モジュールを実装 | `src/mathx.js` |
| 21:31 | テストランナーとテスト61件を実装 | `test/runner.js` `test/mathx.test.js` |
| 21:32 | テストが `canPass` の境界バグを検出 → 許容誤差で修正、全61件成功 | `src/mathx.js` |
| 21:34 | ブラウザ用テストページを追加 | `test.html` `test/run-node.js` |
| 21:23 | メガデモ本体を実装（骨組み・5シーン・音・触れる区間） | `src/*.js` `index.html` `style.css` |
| 21:35 | ゲーム状態とタイムラインのテストを追加、計92件すべて成功 | `test/game.test.js` `test/scenes.test.js` |
| 21:38 | SSH 鍵をパスフレーズなしで再作成（旧鍵のパスフレーズ失念のため） | `~/.ssh` |
| 21:41 | GitHub へ初回 push 成功 | `origin/main` |
